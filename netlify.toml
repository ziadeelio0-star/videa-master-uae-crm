# Netlify Database production migration - npm compatibility
[build]

[build.environment]
  NODE_VERSION = "22.22.0"
  NPM_VERSION = "10.9.3"
  command = "vite build"
  publish = "dist/public"
  functions = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
